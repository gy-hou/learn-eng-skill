# Claude Adapter Notes

Use `../SKILL.md` as the core instruction source.

Adapter-specific room (future):
- conversation style tuning
- tool availability differences
- output verbosity defaults

Must keep contracts aligned with `../references/output-contracts.md`.
