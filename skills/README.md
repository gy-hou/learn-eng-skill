# Skills Layout

- `learn-eng/`: canonical shared skill core.
- `codex/learn-eng`: Codex compatibility entry (symlink to core).
- `claude/learn-eng`: Claude compatibility entry (symlink to core).
- `openclaw/learn-eng`: OpenClaw compatibility entry (symlink to core).

This layout keeps one source of truth while reserving adapter paths for future platform-specific overrides.
